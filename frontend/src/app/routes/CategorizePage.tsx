import StubPage from "./StubPage";

export default function CategorizePage() {
  return (
    <StubPage
      title="Categorize"
      subtitle="Review the queue, handle bulk changes, and keep the ledger clean."
    >
      Prioritize uncategorized transactions and apply bulk actions. Rule suggestions will follow
      in a future sprint.
    </StubPage>
  );
}
