import StubPage from "./StubPage";

export default function AdminSimulatorPage() {
  return (
    <StubPage
      title="Simulator (Admin)"
      subtitle="Scenario catalog and golden-run tooling."
    >
      Admin tools remain hidden from the main navigation. Simulator controls will be added here.
    </StubPage>
  );
}
