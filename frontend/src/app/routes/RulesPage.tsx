import StubPage from "./StubPage";

export default function RulesPage() {
  return (
    <StubPage
      title="Rules"
      subtitle="Automation rules that keep categorization consistent."
    >
      Manage automation rules here. Read-only view for now.
    </StubPage>
  );
}
